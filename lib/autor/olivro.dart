import 'package:flutter/material.dart';

class Olivro extends StatelessWidget {
  const Olivro({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    MainAxisAlignment.spaceEvenly;
    return Container(
      width: 180,
      height: 250,
      decoration: const BoxDecoration(
        color: Colors.white,
      ),
    );
  }
}
